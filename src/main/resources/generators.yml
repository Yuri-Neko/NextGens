# This file intentionally has no generators defined in it.
#
# Generators now live in the 'generators/' folder, one file per dimension:
#   generators/overworld_generators.yml
#   generators/nether_generators.yml
#   generators/end_generators.yml
# (plus generators/elemental_generators.yml and generators/orion_generators.yml,
# NextGens' own original examples)
#
# This file is still loaded on startup (in addition to everything under
# generators/), so you CAN put generators here too if you want - it's just
# no longer where the default/example generators live.
